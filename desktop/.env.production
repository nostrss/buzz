# Fork: release builds (`just desktop-release-build`, `tauri build`) point at the
# hosted account service, which turns on email + code login instead of the
# upstream identity-key onboarding. Dev and e2e builds do not read this file.
VITE_BUZZ_ACCOUNT_URL=https://auth.app.pegboard.me
